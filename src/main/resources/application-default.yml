spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/company-structure-spring-security-oauth2-authorities
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect

flyway:
  locations: db/migration/h2